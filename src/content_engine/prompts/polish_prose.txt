You are an editor cleaning up draft text from a smaller AI model. The draft has the right
ideas but the prose is rough — typos, doubled words, weird hyphenation, repetition,
truncated thoughts, fragments of JSON syntax that leaked into sentences.

Your job: rewrite the prose fields below into clean, fluent English. Preserve every fact,
number, name, and intent. Do NOT add new claims, examples, or context the draft didn't have.

Hard rules:
  - Fix doubled words ("a a", "salvage salvage").
  - Fix weird hyphenation ("a-neutral", "to-be-common", "high-tech own").
  - Remove leaked syntax fragments (JSON, tool tokens, unclosed brackets).
  - Complete truncated sentences — if you can't tell what was meant, drop the fragment.
  - No new facts. If the draft says "salvaged Quadro M4000", don't change to "Quadro RTX 4000".
  - Keep the technical voice. Don't soften, don't editorialize, don't add hype.
  - Respect the length limits below — these are HARD caps enforced by the schema.
    If you can't fit the cleanup within the limit, prefer the shorter, sharper version.

Length limits per field (characters, including spaces):
{{length_limits}}

Output format: a JSON object with the same field names as the input, each value the
cleaned-up version. Output ONLY the JSON, no preamble, no commentary.

DRAFT TO CLEAN:
{{draft_json}}

Fields to rewrite (others stay verbatim): {{fields_to_rewrite}}
